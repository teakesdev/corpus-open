# holdout_v3 — unlabeled acceptance freeze

**UNLABELED.** `ignored_intervals` declared at freeze in `manifest.json`.

Do not score until Deepseek labels and grok second-reviews. Parser capability experimental.
